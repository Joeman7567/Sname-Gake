using System;
using System.Windows.Media;
/*namespace Snake_Gaem
{
    public static class Audio
    {
        public readonly static MediaPlayer fortnut =
            LoadAudio("fortnut.mp3");

        private static MediaPlayer LoadAudio(string filename, bool repeat = false)
        {
            MediaPlayer player = new();
            player.Open(new Uri($"Assets/{filename}", UriKind.Relative));
            return player;
        }
    }
}*/
